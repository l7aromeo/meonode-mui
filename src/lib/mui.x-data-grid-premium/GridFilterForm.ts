import { createMuiNode } from '@src/core.js'
import { GridFilterForm as MuiGridFilterForm } from '@mui/x-data-grid-premium'

const GridFilterForm = createMuiNode(MuiGridFilterForm)
export default GridFilterForm
