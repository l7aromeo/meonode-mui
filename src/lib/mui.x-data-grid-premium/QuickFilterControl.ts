import { createMuiNode } from '@src/core.js'
import { QuickFilterControl as MuiQuickFilterControl } from '@mui/x-data-grid-premium'

const QuickFilterControl = createMuiNode(MuiQuickFilterControl)
export default QuickFilterControl
