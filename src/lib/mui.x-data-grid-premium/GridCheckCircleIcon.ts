import { createMuiNode } from '@src/core.js'
import { GridCheckCircleIcon as MuiGridCheckCircleIcon } from '@mui/x-data-grid-premium'

const GridCheckCircleIcon = createMuiNode(MuiGridCheckCircleIcon)
export default GridCheckCircleIcon
