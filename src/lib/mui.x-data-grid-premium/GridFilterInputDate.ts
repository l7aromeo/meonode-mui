import { createMuiNode } from '@src/core.js'
import { GridFilterInputDate as MuiGridFilterInputDate } from '@mui/x-data-grid-premium'

const GridFilterInputDate = createMuiNode(MuiGridFilterInputDate)
export default GridFilterInputDate
