import { createMuiNode } from '@src/core.js'
import { GridCellCheckboxForwardRef as MuiGridCellCheckboxForwardRef } from '@mui/x-data-grid'

const GridCellCheckboxForwardRef = createMuiNode(MuiGridCellCheckboxForwardRef)
export default GridCellCheckboxForwardRef
