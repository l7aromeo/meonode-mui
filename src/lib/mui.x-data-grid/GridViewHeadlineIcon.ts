import { createMuiNode } from '@src/core.js'
import { GridViewHeadlineIcon as MuiGridViewHeadlineIcon } from '@mui/x-data-grid'

const GridViewHeadlineIcon = createMuiNode(MuiGridViewHeadlineIcon)
export default GridViewHeadlineIcon
