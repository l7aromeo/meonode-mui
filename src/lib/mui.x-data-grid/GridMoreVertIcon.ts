import { createMuiNode } from '@src/core.js'
import { GridMoreVertIcon as MuiGridMoreVertIcon } from '@mui/x-data-grid'

const GridMoreVertIcon = createMuiNode(MuiGridMoreVertIcon)
export default GridMoreVertIcon
