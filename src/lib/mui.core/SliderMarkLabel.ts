import { createMuiNode } from '@src/core.js'
import { SliderMarkLabel as MuiSliderMarkLabel } from '@mui/material'

const SliderMarkLabel = createMuiNode(MuiSliderMarkLabel)
export default SliderMarkLabel
