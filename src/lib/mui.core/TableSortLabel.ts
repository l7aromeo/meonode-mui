import { createMuiNode } from '@src/core.js'
import { TableSortLabel as MuiTableSortLabel } from '@mui/material'

const TableSortLabel = createMuiNode(MuiTableSortLabel)
export default TableSortLabel
