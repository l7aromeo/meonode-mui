import { createMuiNode } from '@src/core.js'
import { DialogContentText as MuiDialogContentText } from '@mui/material'

const DialogContentText = createMuiNode(MuiDialogContentText)
export default DialogContentText
