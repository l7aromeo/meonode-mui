import { createMuiNode } from '@src/core.js'
import { StaticTimePicker as MuiStaticTimePicker } from '@mui/lab'

const StaticTimePicker = createMuiNode(MuiStaticTimePicker)
export default StaticTimePicker
