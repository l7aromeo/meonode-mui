import { createMuiNode } from '@src/core.js'
import { ColumnsPanelTrigger as MuiColumnsPanelTrigger } from '@mui/x-data-grid-pro'

const ColumnsPanelTrigger = createMuiNode(MuiColumnsPanelTrigger)
export default ColumnsPanelTrigger
