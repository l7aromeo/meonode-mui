import { createMuiNode } from '@src/core.js'
import { ToolbarButton as MuiToolbarButton } from '@mui/x-data-grid-pro'

const ToolbarButton = createMuiNode(MuiToolbarButton)
export default ToolbarButton
