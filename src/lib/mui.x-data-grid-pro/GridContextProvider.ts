import { createMuiNode } from '@src/core.js'
import { GridContextProvider as MuiGridContextProvider } from '@mui/x-data-grid-pro'

const GridContextProvider = createMuiNode(MuiGridContextProvider)
export default GridContextProvider
