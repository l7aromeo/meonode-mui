import { createMuiNode } from '@src/core.js'
import { RadarSeriesMarks as MuiRadarSeriesMarks } from '@mui/x-charts'

const RadarSeriesMarks = createMuiNode(MuiRadarSeriesMarks)
export default RadarSeriesMarks
